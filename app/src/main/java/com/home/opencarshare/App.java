package com.home.opencarshare;

public class App {

    public static final String TAG = "TAG";

    public static final String TRIP = "Trip";

    /**
     * TODO
     * - do the check for rotation changes and activity recreation; you can keep the state only within
     * viewmodel or by applying rememberSaveable()
     * - improve UI to production level
     * - validate Google architecture principles: state hoisting, unidirectional dataflow
     * (callbacks via lyambda), reuse of the components
     * - validate for UI and UX consistency across variety of form factors
     * */
}
